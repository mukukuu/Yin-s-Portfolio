import React from "react";
// import Nav from "../Nav";

function Header(props) {
	const { currentTab, setCurrentTab } = props;

	return (
		<header>
			<div>
				<h2>Yin's Portfolio</h2>
			</div>
		
		</header>
	);
}

export default Header;